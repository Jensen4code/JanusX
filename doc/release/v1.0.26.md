# JanusX v1.0.26 Release Notes

Compared with v1.0.25, v1.0.26 started from the `postgwas` visualization workflow and then accumulated a second round of GWAS/GS/REML consistency cleanup before release. In addition to merged plotting, panel-ratio layout consistency, cleaner logging, and better PDF/export behavior for downstream figure editing, this release also aligns phenotype-scale PVE reporting, simplifies FarmCPU metadata handling, and removes the old corrected-sparse-PVE presentation from the REML summary path.

## Added

- Added `-manh-merge` and `-qq-merge` for drawing multiple GWAS result files into a single merged Manhattan or QQ figure, with per-series color/marker mapping and threshold-aware highlighting.
- Added `-marker/--marker` for explicit scatter-symbol control in both single-file and merged plotting modes. In merged mode, markers can cycle across input files.
- Added support for running merged plots and per-file plots in the same `jx postgwas` task, so one command can now produce both combined figures and individual figures together.
- Added `-snp-name/--snp-name` to `jx gformat` for template-based output SNP naming on PLINK/VCF/HMP outputs. By default JanusX keeps the source SNP IDs; templates accept either bare `chr`/`pos` placeholders (for example `chr_pos`, `chr*pos`) or brace placeholders such as `{chr}_{pos}`.

## Changed

- Removed the old `-merge` switch and consolidated merged plotting behavior under `-manh-merge` and `-qq-merge`.
- Reworked postGWAS panel sizing so `-manh`, `-qq`, merged plots, and combined Manhattan+LD figures all follow the same true panel-ratio layout logic based on the drawable plot panel rather than total figure size.
- Updated merged Manhattan/QQ styling so legends are placed at the right side in a single column, threshold-passing points use per-result palettes, and dense merged layers are rasterized while keeping axes/text/legends export-friendly.
- Updated `.postGWAS.log` output to better align with GWAS/GS logging style, including explicit command recording for reproducibility.
- Centralized figure saving and PDF export handling; JanusX now attempts the Cairo PDF backend when available and otherwise falls back automatically to the default Matplotlib PDF backend.
- Restored SciPy (`scipy.stats.beta.ppf`) as the default QQ confidence-band backend for GWAS and postGWAS plotting, so the short-term QQ CI path is again unified across fastplot and postGWAS while keeping exact Beta order-statistic confidence limits.
- Restored the default single-file GWAS QQ sampling thresholds to the denser pre-regression settings: auto mode now switches only above `1,000,000` SNPs, fast-mode scatter keeps up to `120,000` ranks while preserving the most significant tail, and the default QQ confidence-band grid returns to `20,000` Beta-order points. This removes the visible segment breaks introduced by the short-lived `20k/25k/50k` sampling scheme.
- Replaced the lightweight GWAS fastplot phenotype-density overlay with a NumPy implementation, while keeping SciPy available in the plotting stack where it remains the stable/fast choice.
- Renamed GWAS/GS summary and progress labels from generic `PVE` to `PVE(ph)` / `pheno-scale` whenever the reported quantity is phenotype-scale PVE, reducing confusion with raw variance-component ratios.
- Changed GWAS `-global/--global` row-stat reuse to a single in-memory full-sample pass for the current run, instead of persisting `.scanmeta` files to disk.
- Switched FarmCPU main packed loading and QTN packed preload to the GWAS scan-metadata path, so FarmCPU-oriented packed routes no longer depend on the old `.pmeta.*` metadata cache chain.
- Updated the REML narrow-sense reporting path so dense GRM runs use the dense exact REML null fit directly and report phenotype-scale `h2`; the old `PVEc` column/presentation has been removed from the CLI summary tables.
- Standardized GS/Bayes wording and runtime fields around BLUP phenotype-scale PVE, including auto-`r2` messaging and RRBLUP/GBLUP intermediate state names.
- Changed `jx gs` prediction output naming from `{prefix}.{trait}.gs.tsv` to `{prefix}.{trait}.gebv.tsv`, switched BLUP/Bayesian `-save-model` artifacts to text `.jxmodel` effect tables (`chr/pos/snp/beta`), and added a `pip` column for BayesB / BayesC effect exports; ML/scikit-learn model artifacts remain binary.
- Updated `jx postgs` effect discovery to read GS effect tables directly from `.jxmodel`, removing the old `{prefix}.{trait}.gs.effect` side path and keeping the summary/model-artifact linkage inside `summary.json`.
- Reworked GS/GWAS CLI summaries so GWAS now emits one aligned per-trait summary table at trait completion, while GS ends with concise `Finished. Total wall time` / timestamp lines instead of the old final combined summary block.
- Extended SparseLMM `-spk/--grm-sparse` path handling so JanusX now accepts native `.spgrm/.jxgrm` files as before, and also accepts GCTA/fastGWA sparse GRM prefixes or explicit `.grm.sp` paths. When a GCTA sparse GRM is supplied, JanusX imports it once into a sibling `.gcta.spgrm` cache and reuses that converted CSC file automatically on later runs.
- Narrowed the historical `jx gformat --biallelic-only` flag to its intended behavior: it now means "keep only simple single-base A/C/G/T SNP sites" rather than generic "keep all biallelic sites", while `--snps-only` remains the compatibility spelling for the same ATCG-only filter.
- Reworked the `jx gformat` PLINK prefilter + `--prune` path to scan lightweight keep-mask metadata first and then run masked source-BED streaming LD prune per chromosome. This avoids building a full packed intermediate up front, preserves the old streaming prune behavior on the expensive step, and prevents the temporary memory blow-up from whole-payload materialization.
- Noted a small retained helper-level semantic difference on one compared `gformat` prune case: the old helper reported `pre_n/keep_n = 10,233,898 / 842,632`, while the new mask-first streaming helper reported `10,234,918 / 843,022` (`+1,020` pre-prune candidates, `+390` kept sites). This discrepancy predates the packed-memory-path removal and is documented as an existing old-vs-new helper difference rather than a regression introduced by the v1.0.26 streaming-path rewrite.

## Fixed

- Fixed several QQ plotting inconsistencies so `-qq` and `-qq-merge` now respect `-ylim`, `-scatter-size`, `-marker`, and `-palette` even when Manhattan plotting is not requested.
- Fixed QQ confidence-band rendering so the trumpet/band remains grey and is computed only once in merged QQ mode.
- Fixed a macOS/base-environment GWAS visualization crash path that could surface during `Visualizing ...`, especially when multiple GWAS models were plotted in one run, by moving GWAS QQ plotting off the Rust-side QQ helper path and running the visualization stage under single-thread BLAS/Rayon guards to avoid unstable SciPy/OpenMP runtime interactions.
- Fixed Manhattan and QQ axis-tick behavior so Manhattan y ticks and QQ x/y ticks stay on integer values in both single and merged figures.
- Fixed a plotting regression from the short-lived accelerated single-plot path: GWAS `fastplot` and `postgwas` single-file QQ figures now route back through `GWASPLOT.qq()`, and default Manhattan fastplot rendering no longer applies the extra `60k` pre-downsample layer that caused visible gaps/discontinuities in dense datasets.
- Fixed `-bimrange` display clipping so the shown right boundary no longer extends past the actual chromosome or physical interval limit.
- Fixed combined Manhattan+LD figure layout so the top Manhattan panel now uses the same panel-ratio logic as standalone Manhattan plots.
- Fixed merged/single coexistence and related fallback behavior in `postgwas`, reducing duplicated plotting paths and improving task robustness on restricted multiprocessing environments.
- Fixed a small FvLMM vs dense exact REML / `splmm-exact` PVE discrepancy by making the spectral null-model report consume the same phenotype-scale null `pve` field.
- Fixed sparse/dense GWAS null-fit outputs so they now retain both phenotype-scale PVE (`pve_pheno_scale`) and the raw variance-component ratio (`pve_vc_ratio_raw`) for downstream debugging and consistency checks.
- Fixed GWAS mixed-model null fallback consistency so `LMM2` now follows the same null-LRT-to-`LM` downgrade rule as `LMM`, `FvLMM`, and sparse mixed-model routes unless `--force-model` is set. The stream runner now logs the fallback null LRT together with the available phenotype-scale null PVE summary, and when multiple requested stream models collapse to `LM` on the same trait, JanusX reuses the first `LM` scan instead of recomputing duplicate `LM` results.
- Fixed FarmCPU/QTN packed genotype preparation so normal GWAS routes no longer generate dominance-layer or packed-metadata sidecar files such as `.pmeta.dom_af.npy`, `.pmeta.row_maf.npy`, `.pmeta.row_missing.npy`, `.pmeta.row_flip.npy`, `.pmeta.site_keep.npy`, `.pmeta.std_denom.npy`, or related `.pmeta.json`.
- Fixed the raw FarmCPU stage1 controller to follow the rMVP update order (`prior P -> BIN/REM/Remove -> LM/SUB -> next P`) and aligned the packed `FarmCPU.Remove` redundancy pruning rule with rMVP's ordered forward-elimination behavior (`|r| > 0.7`). On the cubic `PH` check used by the benchmark path (`-farmcpu -global`), JanusX now reproduces the rMVP final QTN set exactly (`49/49` exact overlap on the validated case).
- Fixed GS result aggregation so RRBLUP/GBLUP exact, lambda-derived, train-variance, and VC-derived PVE values are tracked separately but reported consistently on the phenotype scale.
- Fixed GS packed/meta loading so BLUP/Bayes packed routes now follow the FarmCPU/GWAS scan-metadata path and no longer emit legacy packed metadata sidecars such as `.pmeta.dom_af.npy`, `.pmeta.row_maf.npy`, `.pmeta.row_missing.npy`, `.pmeta.row_flip.npy`, `.pmeta.site_keep.npy`, or `.pmeta.std_denom.npy`.
- Fixed GS additive GRM cache naming to reuse the same `~prefix.maf*.geno*.snp*.cGRM.npy` namespace as GWAS, including cached BED inputs such as `~prefix.snp0`, so JanusX no longer produces duplicate `~~...` or `~...snp0.maf...` kinship cache variants for the same genotype source.
- Fixed SparseLMM direct sparse-GRM construction from BED so it no longer accumulates long blockwise GRM sums entirely in `float32`. The sparse builder now keeps the BLAS block multiply in `f32` but merges each block into an `f64` accumulator before thresholding/writing, which brings the direct sparse path back into near-entrywise agreement with the dense-then-cut sparse GRM route and removes the severe top-hit drift seen on sensitive RiceAtlas repeats.
- Fixed SparseLMM sparse-GRM cache invalidation for this builder change by versioning the sparse-GRM metadata spec. Existing pre-fix local `.spgrm` files are therefore rebuilt automatically instead of being silently reused after upgrading to v1.0.26.
- Fixed `jx gformat` direct PLINK/VCF/HMP write paths so explicit SNP IDs are honored consistently; inherited IDs and `--snp-name` templates no longer fall back to synthetic `CHR_POS` names on Rust direct-convert routes.

## Performance

- Slimmed in-memory packed/global GWAS scan metadata by removing the persistent `missing_count` array and letting `af`/`maf` share one `float32` buffer whenever they represent the same per-site quantity.
- Verified on the cubic dataset with plotting disabled (`8,665,855` active SNPs in the global row-stat payload) that FvLMM and SparseLMM did not show a runtime regression, while the resident metadata footprint dropped from about `206.6 MiB` to `140.5 MiB` for that payload, saving roughly `66.1 MiB`.
- Replaced the old dense-style GBLUP additive effect reconstruction used for GS export with a Rust PCG backsolve path that reuses the packed BED kernels for effect-table generation, while leaving prediction on the original kernel-based path.
- Benchmarked `jx gformat --snp-name` on the cubic PLINK prune path (`-maf 0.05 --prune 50 5 0.2 --biallelic-only`, `4` threads, capped at `200,000` output sites) and did not observe a measurable runtime regression: baseline runs averaged `23.72 s` versus `22.09 s` with templated SNP renaming.

---

相较于 v1.0.25，v1.0.26 从 `postgwas` 可视化工作流开始，随后又累计了一轮面向正式发布前的 GWAS/GS/REML 一致性清理。除了合并绘图、panel-ratio 布局一致性、更干净的日志输出，以及面向后续图形编辑的 PDF/导出行为改进之外，本版还统一了 phenotype-scale PVE 的命名与尺度、简化了 FarmCPU 的元数据路径，并从 REML 汇总输出中移除了旧的稀疏 PVEc 表达。

## 新增

- 新增 `-manh-merge` 与 `-qq-merge`，可将多个 GWAS 结果文件绘制到同一张 merged Manhattan 或 QQ 图中，并支持按结果文件分配颜色/点形以及阈值以上位点高亮。
- 新增 `-marker/--marker`，用于显式控制单文件模式和 merged 模式下的散点形状；在 merged 模式下可按输入文件循环使用多个 marker。
- 新增在同一次 `jx postgwas` 任务中同时输出 merged 图和逐文件图的能力，因此现在一条命令可以同时产出组合图与单独图。
- `jx gformat` 新增 `-snp-name/--snp-name`，可在输出为 PLINK/VCF/HMP 时按模板重命名 SNP ID。默认继承源文件中的 SNP 名称；模板既支持 `chr_pos`、`chr*pos` 这类裸 `chr`/`pos` 占位形式，也支持 `{chr}_{pos}` 这类花括号占位形式。

## 变更

- 移除了旧的 `-merge` 开关，并将合并绘图统一收敛到 `-manh-merge` 与 `-qq-merge`。
- 重构了 postGWAS 的 panel 尺寸逻辑，使 `-manh`、`-qq`、merged 图以及 Manhattan+LD 组合图都基于真正的绘图区 panel，而不是整张 figure 尺寸，来解释 ratio。
- 更新了 merged Manhattan/QQ 的绘图样式：legend 统一放在右侧单列；阈值以上位点按结果文件使用各自配色；高密度 merged 散点层默认栅格化，同时保留坐标轴、文字和 legend 的后期编辑友好性。
- 更新了 `.postGWAS.log` 的输出风格，使其更接近 GWAS/GS 的日志样式，并增加了命令本身的记录，便于复现。
- 统一了图像保存与 PDF 导出逻辑；当环境中可用时，JanusX 会优先尝试 Cairo PDF 后端，否则自动回退到默认的 Matplotlib PDF 后端。
- 将 GWAS 与 postGWAS 的 QQ 置信带默认后端切回 SciPy（`scipy.stats.beta.ppf`），使短期内主线 QQ CI 路径重新统一到同一套稳定实现，同时继续保留精确的 Beta 顺序统计置信界。
- 将默认单文件 GWAS QQ 图的抽样阈值恢复到回归前的更密集设置：`auto` 模式仅在 SNP 数超过 `1,000,000` 时才切换，fast 模式散点最多保留 `120,000` 个 rank 且继续显式保留最显著尾部，默认 QQ 置信带网格恢复为 `20,000` 个 Beta 顺序统计点。这样去除了短期 `20k/25k/50k` 抽样方案带来的可见断层。
- 将轻量 GWAS fastplot 中 phenotype 分布图的密度覆盖线改为 NumPy 实现，但绘图主链仍保留 SciPy，作为当前更稳定且更快的 QQ CI 计算方案。
- 将 GWAS/GS 中原本泛化的 `PVE` 标签进一步明确为 `PVE(ph)` / `pheno-scale`，仅在实际输出的是 phenotype-scale PVE 时使用该命名，减少与原始方差组分比值的混淆。
- 将 GWAS 的 `-global/--global` 行统计复用方式改为“本次运行内只做一次全样本 row-stat 计算并在内存中复用”，不再向磁盘持久化 `.scanmeta` 文件。
- 将 FarmCPU 主 packed load 与 QTN packed preload 统一切到 GWAS scan metadata 路径，FarmCPU 相关 packed 路线不再依赖旧的 `.pmeta.*` 元数据缓存链。
- 调整了 REML 窄义遗传力的汇报路径：dense GRM 情况下直接使用 dense exact REML null fit，并以 phenotype-scale `h2` 汇报；CLI 汇总表中移除了旧的 `PVEc` 列/表达。
- 统一了 GS/Bayes 中围绕 BLUP phenotype-scale PVE 的术语与运行态字段，包括 auto-`r2` 提示文案，以及 RRBLUP/GBLUP 的中间状态命名。
- 将 `jx gs` 的预测结果命名从 `{prefix}.{trait}.gs.tsv` 改为 `{prefix}.{trait}.gebv.tsv`；同时把 BLUP/Bayesian 的 `-save-model` 产物切换为文本 `.jxmodel` 效应表（`chr/pos/snp/beta`），并为 BayesB / BayesC 的效应导出增加 `pip` 列；ML / scikit-learn 相关模型产物仍保持二进制格式。
- 将 `jx postgs` 的效应文件发现逻辑改为直接从 `.jxmodel` 读取 GS 效应表，不再依赖旧的 `{prefix}.{trait}.gs.effect` 旁路文件，并将模型/效应文件关联统一保留在 `summary.json` 中。
- 调整了 GS/GWAS 的 CLI 汇总输出：GWAS 现在在每个表型结束时立即输出一张对齐后的 summary 表；GS 则改为在结束时只输出精简的 `Finished. Total wall time` 与时间戳，而不再打印旧的最终汇总大表。
- 扩展了 SparseLMM `-spk/--grm-sparse` 的路径兼容性：除了原生 `.spgrm/.jxgrm` 之外，JanusX 现在也接受 GCTA/fastGWA 的 sparse GRM prefix 或显式 `.grm.sp` 路径。传入 GCTA sparse GRM 时，JanusX 会先导入为同目录下的 `.gcta.spgrm` 缓存，后续运行自动复用该转换后的 CSC 文件。
- 将历史命名的 `jx gformat --biallelic-only` 收敛到它原本应有的语义：现在它表示“只保留简单的单碱基 A/C/G/T SNP 位点”，而不是泛指“保留所有双等位位点”；`--snps-only` 继续作为同一 ATCG-only 过滤语义的兼容写法保留。
- 重做了 `jx gformat` 的 PLINK 预过滤 + `--prune` 路径：先扫描轻量 keep-mask metadata，再按染色体对 source BED 做 masked streaming LD prune。这样避免了先整体物化大 packed 中间载荷，昂贵的 prune 步骤仍保持流式处理，也消除了整块 payload 带来的瞬时内存膨胀。
- 额外记录了一处保留下来的 `gformat` prune helper 细小语义差异：在同一份对比用例上，旧 helper 的 `pre_n/keep_n = 10,233,898 / 842,632`，新 mask-first streaming helper 的 `pre_n/keep_n = 10,234,918 / 843,022`，即 prune 前候选位点多 `1,020` 个、最终保留位点多 `390` 个。该差异早于本次 packed 内存路径移除修复就已存在，因此这里将其视为新旧 helper 之间原有的语义差异，并单独说明，而不把它记作 v1.0.26 流式路径重写新引入的回归。

## 修复

- 修复了多处 QQ 绘图不一致问题，使 `-qq` 与 `-qq-merge` 即使在未请求 Manhattan 图时，也能正确受 `-ylim`、`-scatter-size`、`-marker` 和 `-palette` 控制。
- 修复了 QQ 置信带的渲染行为，使喇叭口始终保持灰色，并且在 merged QQ 模式下只计算一次。
- 修复了 macOS/base 环境下 GWAS 在 `Visualizing ...` 阶段的崩溃路径，尤其是多模型连续绘图时可能出现的段错误/中止：当前通过将 GWAS QQ 绘图移出 Rust QQ helper 路径，并在可视化阶段统一加上单线程 BLAS/Rayon 保护，来规避不稳定的 SciPy/OpenMP runtime 交互。
- 修复了 Manhattan 与 QQ 坐标轴 tick 的问题，使 Manhattan 的 y 轴 tick 以及 QQ 的 x/y 轴 tick 在单图和 merged 图中都尽量落在整数值上。
- 修复了短期 accelerated single-plot 路径带来的绘图回归：GWAS `fastplot` 与 `postgwas` 的单文件 QQ 图重新走 `GWASPLOT.qq()` 主路径，默认 Manhattan fastplot 也不再额外叠加那层 `60k` 预抽样，因此在高密度数据上不再出现明显断层/缺口。
- 修复了 `-bimrange` 的显示裁剪问题，防止图中右边界超出真实染色体或物理区间上限。
- 修复了 Manhattan+LD 组合图的布局，使顶部 Manhattan panel 现在与单独 Manhattan 图采用相同的 panel-ratio 逻辑。
- 修复了 `postgwas` 中 merged 与 single 模式共存时的分支与 fallback 行为，减少了重复绘图路径，并提升了在受限多进程环境下的任务稳健性。
- 修复了 FvLMM 与 dense exact REML / `splmm-exact` 之间的一点 PVE 细微偏差，使 spectral null-model 的汇报直接复用相同的 phenotype-scale null `pve` 字段。
- 修复了 sparse/dense GWAS null fit 的输出结构，使其同时保留 phenotype-scale PVE（`pve_pheno_scale`）与原始方差组分比值（`pve_vc_ratio_raw`），便于后续调试与尺度一致性核对。
- 修复了 GWAS mixed-model null fallback 的一致性问题：`LMM2` 现在与 `LMM`、`FvLMM` 以及稀疏 mixed-model 路线一样，在 null LRT 不显著时会默认回退到 `LM`，除非显式指定 `--force-model`。stream runner 现在会把该回退的 null LRT 与可用的 phenotype-scale null PVE 摘要一起写入日志；当同一 trait 下多个 stream 模型都回退到 `LM` 时，JanusX 会直接复用第一次 `LM` 扫描结果，不再重复计算后续的 `LM` 扫描。
- 修复了 FarmCPU/QTN packed genotype 准备路径，使常规 GWAS 路线不再生成 `.pmeta.dom_af.npy`、`.pmeta.row_maf.npy`、`.pmeta.row_missing.npy`、`.pmeta.row_flip.npy`、`.pmeta.site_keep.npy`、`.pmeta.std_denom.npy` 以及相关 `.pmeta.json` 等 sidecar 文件。
- 修复了 raw FarmCPU 的 stage1 控制顺序，使其按 rMVP 的更新链路执行：`prior P -> BIN/REM/Remove -> LM/SUB -> next P`；同时将 packed `FarmCPU.Remove` 的冗余位点裁剪规则对齐为 rMVP 的“按候选顺序前向淘汰，阈值为 `|r| > 0.7`”语义。对 benchmark 默认使用的 cubic `PH` `-farmcpu -global` 检查，JanusX 现已与 rMVP 的最终 QTN 集完全一致（验证用例中为 `49/49` exact overlap）。
- 修复了 GS 结果聚合逻辑，使 RRBLUP/GBLUP 的 exact、lambda、train-variance 与 VC-derived 多种 PVE 现在会分开保留，但统一按 phenotype-scale 解释与汇报。
- 修复了 GS packed/meta load 路径，使 BLUP/Bayes 的 packed 路线也改走 FarmCPU/GWAS 那套 scan-metadata 逻辑，不再落盘生成 `.pmeta.dom_af.npy`、`.pmeta.row_maf.npy`、`.pmeta.row_missing.npy`、`.pmeta.row_flip.npy`、`.pmeta.site_keep.npy`、`.pmeta.std_denom.npy` 等旧式 packed metadata sidecar。
- 修复了 GS additive GRM cache 的命名收敛问题：现在即使输入已经是 `~prefix.snp0` 这类缓存 BED 前缀，也会与 GWAS 统一复用 `~prefix.maf*.geno*.snp*.cGRM.npy` 命名空间，不再为同一份基因型额外生成 `~~...` 或 `~...snp0.maf...` 这类重复 kinship cache。
- 修复了 SparseLMM 从 BED 直接构建 sparse GRM 时的数值累积路径：旧实现会将长链式 block GRM 累积长期保留在 `float32` 中，导致在少数敏感重复上出现严重的 top-hit 漂移。现在改为保留 BLAS block multiply 的 `f32` 计算，但在每个 block 结束后立即并入 `f64` 累积器，再进行阈值筛选和写盘；修复后 direct sparse 路径与 dense-cut sparse GRM 在 entry 级别上重新贴齐，并显著消除了 RiceAtlas 敏感重复中的顶端位点错位。
- 修复了与上述 sparse builder 变更相关的缓存失效逻辑：通过为 sparse-GRM metadata spec 增加版本字段，旧版本地 `.spgrm` 将在升级到 v1.0.26 后自动重建，而不会被静默复用。
- 修复了 `jx gformat` 在 direct PLINK/VCF/HMP 写出路径上对显式 SNP ID 的处理；无论是继承源 ID 还是通过 `--snp-name` 生成的新 ID，都不会再在 Rust direct-convert 路径中退回成合成的 `CHR_POS` 名称。

## 性能

- 瘦身了 packed/global GWAS 路径中的常驻 scan metadata：移除了持久 `missing_count` 数组，并在 `af` 与 `maf` 实际表示相同位点统计量时共享同一份 `float32` 缓冲区。
- 在 cubic 数据上关闭绘图后的实测中（global row-stat payload 含 `8,665,855` 个 active SNP），FvLMM 与 SparseLMM 未见速度回退；对应 metadata 常驻体积约从 `206.6 MiB` 降到 `140.5 MiB`，节省约 `66.1 MiB`。
- 将 GS 导出阶段中 GBLUP additive 效应值的旧式 dense 反推替换为基于 Rust packed BED kernel 的 PCG backsolve 路径，只影响效应表生成，不改变预测阶段原本的 kernel-based 预测主路径。
- 在 cubic 群体的 `jx gformat` PLINK prune 基准上（`-maf 0.05 --prune 50 5 0.2 --biallelic-only`、`4` 线程、输出上限 `200,000` 位点），`--snp-name` 未观察到可测的速度回退：不改名的基线均值约为 `23.72 s`，启用模板改名后的均值约为 `22.09 s`。
